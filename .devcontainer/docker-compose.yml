services:
  my-core-bot:
    image: ghcr.io/42core-team/my-core-bot:dev
    platform: linux/amd64
    volumes:
      - ..:/workspace:cached
      - replays:/workspace/replays
    stdin_open: true

  visualizer:
    image: ghcr.io/42core-team/debug-visualizer:dev
    ports:
      - 4242:80
    volumes:
      - replays:/usr/share/nginx/html/misc

volumes:
  replays:
